//! End-to-end integration test exercising the full SDK pipeline.

#[tokio::test]
#[ignore = "requires AS binary, Redis, and built child binaries — gated"]
async fn full_pipeline_round_trip() {
    panic!("integration test wired up in follow-up; see docs/phase_0_helper_signatures.md");
}
