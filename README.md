# hx_agentic_sdk

Hawcx Agent Authentication Protocol — SDK.

**Status**: alpha rebuild in progress. Option X / Mechanism 2 structure
being populated. See subsequent commits.

Historical reference: pre-Option-X work (PR #1, PR #2) preserved on
the `main-legacy-pre-option-x` branch.
